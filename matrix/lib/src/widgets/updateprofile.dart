import 'package:flutter/material.dart';
class updateurprofile extends StatelessWidget {
  const updateurprofile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
